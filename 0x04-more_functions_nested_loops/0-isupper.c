#include "main.h"

/**
* _isupper - Checks for uppercase characters
* @c: Character
* Return: 1 if c if uppercase, otherwise 0
*/

int _isupper(int x)
{
	if (x >= 'A' && x <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
